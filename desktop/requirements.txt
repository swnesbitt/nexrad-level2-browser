# Desktop (local macOS) build of the NEXRAD Level 2 browser.
#
# The app's dependencies are defined ONCE in ../requirements.txt (shared with
# the Hugging Face Space) so the two builds never drift. Here we only add the
# native-window wrapper.

-r ../requirements.txt

# native window (macOS uses the built-in WKWebView via pyobjc)
pywebview
pyobjc-framework-Cocoa ; sys_platform == "darwin"
pyobjc-framework-WebKit ; sys_platform == "darwin"
